# Full end to end process
python -m src --mode development
python -m src --mode production

# Reset
python utilities/reset_migration.py --all

# Manual Copy
python utilities/generate_create_table.py utilities/SYSCOLUMNS_202601231305.csv PS_INSTALLATION PSF8DEV


#TODO
Indexes
Views

#DONE
[x] True row-by-row streaming with ibm_db implemented in ExtractionCursor (uses ibm_db.fetch_tuple())
[x] Minimize logging (--log-level WARNING)

# Run Time Notes
The sweet spot for batch size is typically 5,000-10,000 rows as you mentioned. Larger batches provide diminishing returns and can cause memory pressure.